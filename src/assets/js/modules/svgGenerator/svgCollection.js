/* eslint-disable no-underscore-dangle */

const svgCollection = (() => {
  let _collection;

  // Add svg parent or child to collection
  // Set attributes for collection


  const addToCollection = () => {
    return -1;
  };

  return {
    addToCollection,
  };
})();
export { svgCollection };
